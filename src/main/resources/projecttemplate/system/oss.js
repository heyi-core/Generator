module.exports = {
  region: 'oss-cn-beijing', // 例如：oss-cn-beijing
  accessKeyId: '**',
  accessKeySecret: '**',
  bucket: 'sapi-sys'
  // bucket: process.env.NODE_ENV === 'production' ? 'jinbeijie-static' : 'jinbeijie-static'
}
